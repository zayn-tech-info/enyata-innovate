const express = require('express');
const router = express.Router();
const circlesController = require('../controllers/circles.controller');

// router.get('/', circlesController.getAll);

module.exports = router;
