const express = require('express');
const router = express.Router();
const contributionsController = require('../controllers/contributions.controller');

// router.post('/', contributionsController.create);

module.exports = router;
